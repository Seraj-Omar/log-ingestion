Ingestion       >= 15,000 logs/sec
Database        ~1,000,000+ logs
Aggregation     < 1s p95
Freshness (new data becomes queryable within)       < 20 seconds
Query workload  1 aggregation/sec during ingestion

Application:
0.5 CPU
256 MB RAM

PostgreSQL:
1 CPU
1 GB RAM
